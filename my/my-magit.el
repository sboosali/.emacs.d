(provide 'my-magit) 
;; (require 'magit)

;; (magit-toggle-section)

;; (add-hook 'magit-mode-hook (lambda()
;;  (define-key (current-local-map) "<tab>" nil)))

;; http://stackoverflow.com/questions/2736087/eval-after-load-vs-mode-hook
;; (eval-after-load "magit" '(progn
;;  (define-key magit-mode-map "C-x C-y h" 'magit-toggle-section))
;;  (define-key magit-mode-map "<tab>" 'magit-toggle-section))
;; )

