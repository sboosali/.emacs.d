# -*- mode: snippet -*-
# key: iqp
# name: qualified package import
# condition: (= (length "iqp") (current-column))
# contributor: Luke Hoersten <luke@hoersten.org>
# commentary: `(progn (require 'haskell-yas) (haskell-yas-complete "Package: " haskell-yas-stackage-packages))`
#
# --
import qualified "${3}" ${1:MODULE} as ${2:${1:$(let ((name (car (last (split-string yas-text "\\\.")))))
