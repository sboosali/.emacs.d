# -*- mode: snippet -*-
#
# key         : defnat
# name        : a « defcustom » declaration for non-negative « (:integer ...) » types.
# condition   : (let ((KEY "defnat")) (condition-case nil (sboo-yasnippet-condition :key KEY :indentation 6) (void-function (= (current-column) (string-width KEY)))))
# expand-env  : ((yas-indent-line 'fixed) (yas-wrap-around-region 'nil))
# contributor : Spiros Boosalis <samboosalis@gmail.com> 
#
# --
;;----------------------------------------------;;

(defcustom sboo-${1:name}

  ${2:nil}

  "$0.

(a natural.)"

  :type '(choice (const nil :tag "Unknown")
                 (integer :tag "Natural"))

  :safe #'integerp
  :group 'sboo)

