# -*- mode: snippet -*-
#
# key         : defcolor
# name        : a « defcustom » declaration for « :color » types.
# condition   : (let ((KEY "defcolor")) (condition-case nil (sboo-yasnippet-condition :key KEY :indentation 0) (void-function (= (current-column) (string-width KEY)))))
# expand-env  : ((yas-indent-line 'fixed) (yas-wrap-around-region 'nil))
# contributor : Spiros Boosalis <samboosalis@gmail.com> 
#
# --
;;----------------------------------------------;;

(defcustom sboo-${1:name}-color

  ${2:"black"}

  "$0.

a \`stringp'."

  :type '(color :tag "${3:Color}")

  :safe #'stringp
  :group 'sboo)

