;;; sboo-early-init.el --- @sboosali's ‘early-init.el’ -*- coding: utf-8; lexical-binding: t; -*-

(require 'package)
;;TODO (setq package-enable-at-startup nil)

;; `package-initialize' will be called halfway down `init.el';
;; the external-packages will be installed/activated/configured only after internal-packages have been configured.

;; (provide 'sboo-early-init)
;;; sboo-early-init.el ends here